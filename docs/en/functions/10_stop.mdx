---
sourceSHA: 9fff9f5e270049f591b68f8c58a5c921ec8f61b7a80eab59c21100361bde24a3
---

# Stop/Start Instance

## Function Overview

The one-click stop/start feature meets the needs of day-to-day development and operations activities, allowing you to quickly pause or start middleware instances. In scenarios such as planned downtime, excessive node load, or development and testing environments, you may need to temporarily release CPU and memory resources without deleting the instance. The one-click stop/start feature helps you save resources and flexibly manage middleware instances, thereby reducing costs and increasing efficiency.

## Prerequisites

Stopping an instance requires the instance to be in **Processing**, **Running**, **Abnormal**, or **Unknown** status.

Starting an instance requires the instance to be in **Stopping** or **Stopped** status.

## Notes

It is recommended to perform a full backup before stopping the instance to prevent data loss.

## Operation Steps

<Tabs>
  <Tab label="CLI">
    #### Stop Instance

    ```bash
    kubectl patch mysql -n <namespace> <name> --type='merge' --patch '
    spec:
        pause: true
    '
    ```

    #### Start Instance

    ```bash
    kubectl patch mysql -n <namespace> <name> --type='merge' --patch '
    spec:
        pause: false
    '
    ```
  </Tab>

  <Tab label="Web Console">
    #### Stop Instance

    1. Click on **MySQL-PXC** in the left navigation bar.

    2. Click on ***Namespace Name***.

    3. Click on ***Instance Name***.

    4. Click on **Operations** > **Stop**, and confirm.

    ### Features Unavailable After Stopping

    | Feature        | Description              |
    | --------- | --------------- |
    | **Terminal Console** | Terminal console is not accessible.      |
    | **Operations**    | Cannot restart, update, or update scheduling configuration. |
    | **Topology**    | Topology page cannot be viewed.        |
    | **Parameter Configuration**  | Cannot update parameters or export parameters.    |
    | **User Management**  | User management is not possible.       |
    | **Backup and Recovery**  | Backup cannot be executed.         |
    | **Inspection**    | Inspection results for the current instance are invalid.   |

    #### Start Instance

    1. Click on **MySQL-PXC** in the left navigation bar.

    2. Click on ***Namespace Name***.

    3. Click on ***Instance Name***.

    4. Click on **Operations** > **Start**, and confirm.
  </Tab>
</Tabs>
